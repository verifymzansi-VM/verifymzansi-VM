export const metadata = {
  title: "Edit Business",
  description: "Update your Mzansi Business profile, details, and media.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
