export const metadata = {
  title: "Edit Promotions & Events Post",
  description: "Update your Promotions & Events post details on VerifyMzansi.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
