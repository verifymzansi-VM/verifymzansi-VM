export const metadata = {
  title: "Edit Promotion",
  description: "Update your promotion or event ad details on VerifyMzansi.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
