export const metadata = {
  title: "Edit Listing",
  description: "Update your Mzansi Market listing details, images, and pricing.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
