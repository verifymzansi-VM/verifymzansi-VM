export const metadata = {
  title: "Settings",
  description: "Manage your VerifyMzansi account settings and preferences.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
