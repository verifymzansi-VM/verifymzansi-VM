export const metadata = {
  title: "Checkout",
  description: "Complete your VerifyMzansi plan purchase via PayFast.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
