export const metadata = {
  title: "Identity Verification",
  description:
    "Complete your 4-step identity verification on VerifyMzansi — phone, ID, selfie, and location.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
